@{
    ViewData["Title"] = "Library";
}

@model IEnumerable<Games>

<h4>@TempData["success"]</h4>

<div class="text-center">
    <h1 class="display-4">@ViewData["Title"]</h1>
</div>

@if (Model == null)
{
	<div class="text-center">
		<h2>No Games</h2>
	</div>
}
else
{
	foreach (Games game in Model)
	{
		<div class="text-center">
			<partial name="_GameLibrary.cshtml" for="@game" />
		</div>
	}
}
